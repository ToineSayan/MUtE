dataset: GloVe
output_dir: outputs/GloVe

# primary erasure model 
erasure_model:
  name: FaRM

erasure_model_init_params:
  io_size: 300
  hidden_size: null
  num_layers: 4
  n_epochs: 500
  batch_size: 512
  learning_rate: 1.0e-3
  weight_decay: 1.0e-5
  seed: 123

seed: 123
